package main

import (
	"encoding/json"
	"log"
	"net/http"
	"time"
)

type Time struct {
	Time string `json:"time"`
}

func timeHandler(w http.ResponseWriter, r *http.Request) {
	
}

func main() {
	
}
